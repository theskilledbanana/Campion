{
  "entities": {
    "LeaderboardEntry": {
      "title": "Leaderboard Entry",
      "description": "Represents a player's achievement in the portal.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique Firebase Auth UID of the player."
        },
        "username": {
          "type": "string",
          "description": "The display name chosen by the player."
        },
        "playTime": {
          "type": "integer",
          "description": "Total cumulative seconds spent playing."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last time the entry was updated."
        }
      },
      "required": ["userId", "username", "playTime", "updatedAt"]
    }
  },
  "firestore": {
    "/leaderboard/{userId}": {
      "schema": "LeaderboardEntry",
      "description": "Global leaderboard store where each user has one entry keyed by their UID."
    }
  }
}
